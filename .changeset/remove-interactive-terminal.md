---
"@kilocode/cli": major
"@kilocode/sdk": major
---

Remove the `interactive_terminal` tool, in-session terminal controls, and related API endpoints. Run commands that need keyboard input in your own terminal instead.
