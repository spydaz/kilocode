---
"kilo-code": minor
---

Make multi-project Agent Manager available by default, with the current workspace project first, and remove the experimental toggle and old single-project view.

Keep restored tabs, background sessions, and branch defaults with their project, and restore the remaining project after removing the active project.
